import Button from "../Buttons/Button";

const ReduxComponent = () => {
    return (
        <div>
            <Button back />
            <p>Page to provide the different steps, changes in the new version, gotchas and pitfalls when implementing redux newest version.</p>
        </div>
    )
}

export default ReduxComponent;
